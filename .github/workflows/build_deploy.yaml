name : blogAPI CI/CD

on :
  push :
    branches :
      - "main"
      - "master"

jobs :
  build :
    runs-on : ubuntu-latest
    steps :
      - name : Push repo
        uses : Debiloss/Project_7
      - name : Print hello
        run : echo "Hello world"